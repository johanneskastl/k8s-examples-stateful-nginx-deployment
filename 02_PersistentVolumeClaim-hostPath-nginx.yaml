---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-hostpath-volumeclaim
spec:
  storageClassName: manual
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
